# User Preferences

- For recurring automations, default to a cloud runner or hosted workflow.
- Do not rely on the local machine staying powered on or connected.
- Keep credentials in secret storage, never in code or chat.
- Verify manual dispatch before trusting a scheduled run.
- If a cloud path fails, fix the cloud path instead of switching to a local-only workaround.

